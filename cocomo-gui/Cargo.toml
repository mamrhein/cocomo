# ---------------------------------------------------------------------------
# cocomo_gui — Desktop GUI for directory comparison
# ---------------------------------------------------------------------------

[package]
name = "cocomo-gui"
version = "0.0.1"
edition = "2024"
authors = ["Michael Amrhein <michael@adrhinum.de>"]
description = "GUI-App: COmpare, COpy & MOve directories and files."
readme = "../README.md"
homepage = "https://github.com/mamrhein/cocomo"
repository = "https://github.com/mamrhein/cocomo"
documentation = "https://docs.rs/cocomo-gui/"
license-file = "../LICENSE.TXT"
keywords = ["directory", "file", "compare", "copy", "move"]
categories = ["gui", "filesystem"]

[[bin]]
name = "cocomo-gui"
path = "src/main.rs"

[dependencies]
cocomo-lib = { path = "../cocomo-lib" }
gpui = { git = "https://github.com/zed-industries/zed", package = "gpui", version = "*" }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
anyhow = "1"
log = "0.4"
dirs = "6"

[target.'cfg(target_os = "linux")'.dependencies]
gpui_platform = { git = "https://github.com/zed-industries/zed", package = "gpui_platform", version = "*", features = ["wayland"] }

[target.'cfg(target_os = "macos")'.dependencies]
gpui_platform = { git = "https://github.com/zed-industries/zed", package = "gpui_platform", version = "*", features = ["font-kit"] }
